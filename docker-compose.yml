services:
  app:
    container_name: locketdio-exten-media
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-5025}:5025"
    environment:
      - PORT=${PORT:-5025}
    restart: unless-stopped